name: build 
# This workflow is triggered on pushes to the repository.
on: [push]

jobs:
  build: 
    name: build-linux
    runs-on: ubuntu-latest
    container: nxxm/nxxm-ubuntu-staging-277
    env:
      NXXM_AUTH: ${ secrets.NXXM_AUTH }
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: nxxm builds project 
        run: |
          export HOME=/root
          nxxm . -t gcc-7-cxx17 --dont-upgrade --verbose --test all 
